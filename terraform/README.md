# Terraform cloud-ready ????

??? ????? ???????? Terraform-????????? ??? ???????? ????????????? ??????? `java-performance-demo` ? ??????.

Terraform ???????? ?? ?????????? ??????????????:

* ????;
* ???????;
* security group / firewall rules;
* ??????????? ??????;
* boot disk;
* public IP;
* SSH-??????.

Terraform **?? ????????????? Kubernetes manifests**.
Kubernetes-??????? ???????? ? ????? `k8s/`, ? Ansible ???????? ?? ????????? Docker/k3s ? ?????????? manifests.

## ???? Terraform ? ???????

??????? ????????? ?????:

```text
VirtualBox VM
  -> Ansible
      -> Docker / k3s
      -> Kubernetes manifests
          -> Spring Boot app
          -> PostgreSQL
          -> Redis
          -> Prometheus
          -> Grafana
```

Cloud-ready ?????:

```text
Terraform
  -> Cloud VM, network, subnet, security group, disk, public IP
  -> Ansible
      -> Docker / k3s installation
      -> Kubernetes manifests apply
          -> Spring Boot app
          -> PostgreSQL
          -> Redis
          -> Prometheus
          -> Grafana
```

## ??? ??? ????????????

?????? ????????? ???????????? ??? Yandex Cloud:

```text
terraform/environments/yandex
```

?????????:

```text
terraform/
+-- README.md
+-- environments/
¦   +-- yandex/
¦       +-- main.tf
¦       +-- variables.tf
¦       +-- outputs.tf
¦       +-- terraform.tfvars.example
¦       +-- .gitignore
+-- modules/
    +-- compute-vm/
        +-- main.tf
        +-- variables.tf
        +-- versions.tf
        +-- outputs.tf
```

## ??? ??????? Terraform

? ??????? ?????? Terraform ?????????:

```text
VPC network
Subnet
Security Group
Virtual Machine
Boot Disk
Public IP
SSH access
```

????? ???????? VM Ansible ????? ???????????? ? ??? ?? SSH ? ?????????? ?????.

## ??? Terraform ?? ??????

Terraform ?? ???????:

```text
Kubernetes Deployment
Kubernetes Service
Kubernetes ConfigMap
Kubernetes PVC
Grafana dashboards
Prometheus config
```

??? ??????? ??? ??????? ? ?????:

```text
k8s/
```

????? ?????????? ??????? ??????????:

```text
Terraform   -> infrastructure
Ansible     -> server setup / automation
Kubernetes  -> application and monitoring stack
```

## ???????? ??? ??????? cloud

????? ????????? ?????????????? Terraform-???? ??? ???????? ???????? ????????:

```bash
cd ~/training/java-performance-demo

terraform fmt -recursive
```

???? Terraform Registry ???????? ?? ??????? ????, ????? ????? ?????????:

```bash
cd terraform/environments/yandex

terraform init
terraform validate
```

## ?????? ?????????? ??? Terraform Registry

`terraform init` ??????? ?????? ? Terraform Registry, ????? ??????? Yandex Cloud provider:

```text
yandex-cloud/yandex
```

???? ?? ??????? ???? `registry.terraform.io` ?????????? ??? ??????????? ?? ?????????, `terraform init` ????? ??????????? ???????.

?????? ??????:

```text
Invalid provider registry host
x-amzn-waf-reason: geo
```

??? ?? ?????? Terraform-???? ? ?? ???????? ???????.
? ????? ?????? ???? ????????? ?????????:

```text
1. ????????? terraform init ?? ?????? ????.
2. ???????????? VPN.
3. ???????????? ????????? ??????? Terraform providers.
4. ???? ???????????? terraform fmt -recursive ? ??????? Terraform ??? cloud-ready ????.
```

? ??????? ?????? ??????? cloud ?? ???????????? ?????????, ??????? Terraform ???????? ??? ?????????????? Infrastructure as Code ???? ??? ??????? ???????? ? ??????.

## ?????????? ? ????????? ???????

??????????? ?????? ??????????:

```bash
cp terraform.tfvars.example terraform.tfvars
```

????????? ???????? ????????:

```hcl
cloud_id  = "your-yandex-cloud-id"
folder_id = "your-yandex-folder-id"
zone      = "ru-central1-a"

image_id = "your-ubuntu-image-id"
```

????? ????? ????? ?????????:

```bash
terraform init
terraform plan
terraform apply
```

????? `terraform apply` Terraform ???????:

```text
vm_public_ip
vm_internal_ip
vm_id
ssh_command
ansible_inventory_line
```

??????:

```text
ssh_command = "ssh ubuntu@x.x.x.x"
```

???? IP ????? ???????????? ? Ansible inventory.

## ?????? ??????????? ??????? Ansible

????? ???????? VM ????? Terraform:

```bash
cd ansible

ansible-playbook -i inventory.ini playbooks/install-docker.yml
ansible-playbook -i inventory.ini playbooks/install-k3s.yml
ansible-playbook -i inventory.ini playbooks/k8s-up.yml
```

## ??????? ????????

????? ?? ??????? ?? ????????????? ??????????????:

```bash
terraform destroy
```

## Storage notes

? ??????? ????????? ?????? ?????? ???????? ????? PVC ?????? k3s.

??? production-like ????????:

```text
PostgreSQL live data  -> PVC / cloud disk
Prometheus TSDB       -> PVC / cloud disk
Grafana runtime data  -> PVC / cloud disk
```

S3/Object Storage ?? ???????????? ??? live-volume ??? PostgreSQL ??? Prometheus.

S3/Object Storage ????? ???????? ????? ???:

```text
PostgreSQL backups
Prometheus long-term metrics ????? Thanos / Mimir / VictoriaMetrics
artifact storage
```

## ??????? ??????

?????? Terraform-???? ???????? ??? cloud-ready ????? ???????.

????????? ??????????? ?????????? ???????? ?????:

```text
VirtualBox VM
Ansible
k3s
Kubernetes manifests
```

Cloud-???????? ????? ????? ???????? ?????, ???? ??????????? ???????? ????????????? ? Yandex Cloud.
